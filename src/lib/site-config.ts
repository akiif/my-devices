// Single source of truth for the GitHub Pages sub-path this site is served from.
// Used by next.config.ts (to configure the build) and by components that build
// URLs for files in `public/` by hand, since Next only rewrites basePath
// automatically for `next/image`, `next/link`, and metadata files.
export const basePath = "/my-devices";
